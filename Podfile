# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CustomNotificationContent' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomNotificationContent

end

target 'CustomNotificationService' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomNotificationService

end

target 'CustomPushNotification' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomPushNotification

  pod 'Swifter', '~> 1.4.5'
  pod 'NWPusher', '~> 0.7.0'

  target 'CustomPushNotificationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
